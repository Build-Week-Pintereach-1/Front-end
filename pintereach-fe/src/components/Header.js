import React from 'react'
import Nav from './Nav'

function Header () {
    return (
        <header>
            <p>I am a header, maybe with a header image!!</p>
            <Nav />
        </header>
        
    )
}

export default Header;